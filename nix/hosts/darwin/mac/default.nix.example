# Mac host-specific overrides (OPTIONAL)
# Base configuration (hostname, user, etc.) is handled automatically by the flake
{
  config,
  pkgs,
  lib,
  ...
}: let
  username = config.system.primaryUser; # Use the primary user set by the flake
in {
  # Mac-specific applications (work-focused)
  homebrew.casks = [
    "slack"
    "microsoft-teams"
    "microsoft-office"
    # Add work-specific apps here
  ];

  homebrew.brews = [
    # Add work-specific CLI tools here
  ];

  # Mac-specific system packages
  environment.systemPackages = with pkgs; [
    # Add work-specific packages here
  ];

  # Mac-specific dock configuration (work-focused)
  system.defaults.dock.persistent-apps = [
    "/Applications/Brave Browser.app"
    "/Applications/Microsoft Teams.app"
    "/Applications/Slack.app"
    "/Applications/Microsoft Excel.app"
    "/Applications/Obsidian.app"
    # Add work-specific dock apps here
  ];

  # Mac-specific system defaults (work environment)
  system.defaults = {
    dock.tilesize = 32; # Smaller dock for work
    dock.autohide = true; # More screen space for work
    # Add other work-specific overrides here
  };
}
